// Runtime product identity. package.json contributions mirror these values and
// must remain literal because VS Code reads the manifest without loading code.
export const BRAND = 'TupleBase'
export const PRODUCT_ID = 'tuplebase'
export const CONFIG_FILENAME = '.tuplebase.json'
